import React from 'react';
import PropTypes from 'prop-types';
import { acceptMember } from '../../action/profileActions'

class FriendsRequest extends React.Component {
  render () {
    return (
      <div>Ces personnes vous ont récemment envoyé une demande de connexion </div>
    )
  }
}

export default FriendsRequest;
